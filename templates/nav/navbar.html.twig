<nav class="navbar navbar-expand-lg bg-white shadow p-1 rounded">
    <div class="container">
        <a class="navbar-brand" href="{{path('home')}}">
            <img src="{{asset('uploads/nav/logo.png')}}" height="35" alt="logo">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">

            <div class="mx-auto">
                <form class="d-flex" role="search" action="{{ path('search') }}" method="get">
                    <input class="form-control me-2" name="query" type="search" placeholder="Rechercher..." aria-label="Search">
                    <button class="btn btn-outline-secondary" type="submit"><i class="fa-solid fa-magnifying-glass"></i></button>
                </form>
            </div>

            <div class="nav-item dropdown">
                {% if app.user %}
                    <a class="" href="{{ path('app_user_account') }}">
                        <img src="{{asset('uploads/nav/' ~ app.user.photo)}}" height="50" alt="{{app.user.username}}">
                    </a>
                    <p>{{app.user.username}}</p>
                    {# <div class="dropdown-menu" aria-labelledby="dropdownMenuButton1" style="position: absolute; transform: translate3d(0px, 37px, 0px); top: 0px; left: 0px; will-change: transform;" x-placement="bottom-start">
                        <a class="dropdown-item text-danger" href="{{ path('app_logout') }}">Déconnexion</a>
                    </div> #}
                {% else %}
                    <a aria-expanded="true" aria-haspopup="true" class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button">
                        Connexion / inscription</a>
                    <div class="dropdown-menu" style="position: absolute; transform: translate3d(0px, 37px, 0px); top: 0px; left: 0px; will-change: transform;" x-placement="bottom-start">
                        <a class="dropdown-item" href="{{ path('app_login') }}">Connexion</a>
                        <a class="dropdown-item" href="{{ path('app_register') }}">S'inscrire</a>
                    </div>
                {% endif %}
            </div>
        </div>
    </div>
</nav>



